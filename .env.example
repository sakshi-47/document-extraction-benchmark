# Copy to a local dotenv file and fill in. That file is gitignored.
# On Kaggle these come from Add-ons -> Secrets instead, never from a cell.

HF_TOKEN=
ANTHROPIC_API_KEY=

DOCFIT_DATA_DIR=./data
DOCFIT_OUTPUT_DIR=./outputs
DOCFIT_SPLIT_SALT=docfit-v1
